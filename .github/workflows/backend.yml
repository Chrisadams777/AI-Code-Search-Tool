# .github/workflows/backend.yml
name: Backend CI/CD

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '1.18'
      - run: go test ./...
      - name: Build Docker Image
        run: docker build -t ghcr.io/Chrisadams777/AI-Code-Search-Tool/backend:latest .
      - name: Push Docker Image
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u Chrisadams777 --password-stdin
          docker push ghcr.io/Chrisadams777/AI-Code-Search-Tool/backend:latest

